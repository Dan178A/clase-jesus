# 1. Crea y retorna una matriz de 3x3 con todos sus elementos en cero.
def matriz_ceros_3x3():
    matriz = []
    return matriz




# 2. Crea y retorna una matriz de 4x4 con 1 en la diagonal principal y 0 en el resto.
def matriz_identidad_4x4():
    matriz = []
    return matriz

# 3. Pide al usuario el número de filas y columnas y llena la matriz con números ingresados por el usuario.
def matriz_usuario():
    filas = int(input("Número de filas: "))
    columnas = int(input("Número de columnas: "))
    matriz = []
    return matriz

# 4. Recibe una matriz y devuelve la suma de todos sus elementos.
def suma_elementos(matriz):
    suma = 0
    return suma

# 5. Recibe una matriz y devuelve una nueva matriz con los valores multiplicados por 2.
def matriz_por_dos(matriz):
    nueva = []
    return nueva

# 6. Recibe dos matrices del mismo tamaño y devuelve su suma.
def suma_matrices(A, B):
    filas = len(A)
    columnas = len(A[0])
    resultado = []
    return resultado

# 7. Recibe una matriz cuadrada y devuelve una lista con los elementos de la diagonal principal.
def diagonal_principal(matriz):
    diagonal = []
    return diagonal

# 8. Recibe una matriz y devuelve la matriz transpuesta.
def transpuesta(matriz):
    filas = len(matriz)
    columnas = len(matriz[0])
    resultado = []
    return resultado